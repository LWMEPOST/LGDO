# LGDO Obsidian Vault

Knowledge pages under `wiki/` are editable. Treat generated `indexes/` and `logs/` as read-only, and use `reviews/` for the review workflow.

The starter file under `templates/` is copied only when missing. A deliberate refresh backs up local changes before restoring the distributed version.

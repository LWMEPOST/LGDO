# 更新说明

## 2026-07-03

### 登录与账户 ACL 管理

- 新增本地账户登录：默认引导管理员为 `admin / admin`，登录后使用 Bearer session token 访问内部接口。
- 新增账户 ACL 管理页面：管理员可以创建账户、修改角色、启用/禁用账户、重置密码、维护 ACL 标签。
- QA 页面改为展示当前登录身份，不再允许前端手工覆盖 `user_id`、`role`、`acl_tags`。
- 内部控制台接口补齐鉴权：关闭开发回退后，未认证请求返回 `401`；管理写操作按 `admin/editor` 做权限门禁。
- 上传接口已补充 Bearer token 透传，避免登录后文件上传被后端拒绝。

### 数据库与迁移

- 新增 `accounts` 与 `auth_sessions` 表，支持 SQLite 与 PostgreSQL 后端。
- SQLite 到 PostgreSQL 迁移已纳入账户与会话表字段白名单。
- 新增管理员自锁保护：禁止当前管理员禁用或降权自己，避免失去管理入口。

### 前端更新

- 新增登录页和账户权限页，沿用现有管理台风格。
- 顶部栏展示当前登录用户与角色，并提供退出登录。
- 账户编辑时切换账号会清空临时密码输入，避免误改其他账户密码。

### 验证记录

- `python -m pytest -q`：`81 passed`
- `npm run build`：通过
- `python -m py_compile app\accounts.py app\api.py app\auth.py app\config.py app\db.py app\models.py app\migration.py tests\test_accounts_auth.py`：通过
- `git diff --check`：通过
- 临时后端 HTTP 验证：`/api/internal/auth/login`、`/api/internal/accounts`、`/console` 可用

### 后续计划

- 将 session token 改为哈希存储。
- 细化账户管理错误码，例如重复账户、账户不存在、权限保护冲突。
- 增加登录、创建账户、保存 ACL、退出登录等浏览器级 E2E 测试。
- 补充 OIDC 生产映射策略、登录失败限流和密码策略。
